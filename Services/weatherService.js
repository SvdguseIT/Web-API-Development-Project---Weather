// server/services/weatherService.js
const Weather = require('../models/Weather');

const weatherService = {
  // Weather service methods (e.g., getAll, getByDistrict, create)
};

module.exports = weatherService;
