// server/services/authService.js
const jwt = require('jsonwebtoken');
require('dotenv').config();

const authService = {
  // Authentication service methods (e.g., generateAccessToken)
};

module.exports = authService;
