// server/controllers/weatherController.js
const Weather = require('../server/models/Weather');

const weatherController = {
  // Weather controller methods (e.g., getAllWeatherData, getWeatherDataByDistrict, createWeatherData)
};

module.exports = weatherController;
